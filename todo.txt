make components dumb structs to reduce libs used by dylib
move specs component impl into separate lib

move systems dumb structs to reduce libs used by dylib
move specs system impl into separate lib

add input checks to quit program

add input checks to move symbol

add window bound check for symbol movement

figure out how to cache dylib symbols?

figure out how not to have to type dylib function signature in 3 places?
